using Calculadora.Entities;

Calcular calculo = new Calcular();

// calculo.Somar(5, 5);
// calculo.Subtrair(15,50);
// calculo.Multiplicar(5,6);
// calculo.Dividir(2,4);
// calculo.Potencia(3,3);
calculo.Seno(30);
calculo.Coseno(30);
calculo.Tangente(30);
